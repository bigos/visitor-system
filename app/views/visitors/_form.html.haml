= form_for @visitor do |f|
  - if @visitor.errors.any?
    #error_explanation
      %h2= "#{pluralize(@visitor.errors.count, "error")} prohibited this visitor from being saved:"
      %ul
        - @visitor.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :arrived_at
    = f.datetime_select :arrived_at
  .field
    = f.label :host_id
    = f.text_field :host_id
  .field
    = f.label :host_name
    = f.text_field :host_name
  .field
    = f.label :visit_purpose
    = f.text_area :visit_purpose
  .actions
    = f.submit 'Save'
